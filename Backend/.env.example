# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=stride_db
DB_PASSWORD=postgres
DB_PORT=5432

# JWT Authentication Configuration
JWT_SECRET=super_secret_jwt_key_stride_2026
JWT_EXPIRES_IN=7d

# Authentication Toggle:
# Set to 'false' to temporarily disable authentication for testing/development
# Set to 'true' to enable strict JWT verification and role authorization
ENABLE_AUTH=false
