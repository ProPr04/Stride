# Frontend Configuration
VITE_API_URL=http://localhost:5000/api

# Authentication Toggle:
# Set to 'false' to disable authentication checks and route guards during UI development
# Set to 'true' to enforce real login, password validation, and JWT route protection
VITE_ENABLE_AUTH=true
